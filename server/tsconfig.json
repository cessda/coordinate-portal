{
  "extends": "../tsconfig.node.json",
  "compilerOptions": {
    // Allows JSON to be loaded as a module
    "resolveJsonModule": true
  },
  "include": ["**/*", "**/*.json"],
  "references": [
    { "path": "../common" }
	]
}
